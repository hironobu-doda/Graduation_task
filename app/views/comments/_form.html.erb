<% comment.date = creation_date.date %>

<%= form_with(model: [creation_date, comment]) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p><%= form.label :before_content %></p>
    <p><%= form.text_area :before_content, id: :comment_before_content, size: "50x6" %></p>
  </div>

  <div class="field">
    <p><%= form.label :before_point %></p>
    <p><%= form.text_area :before_point, id: :comment_before_point, min: 0, max: 100, size: "6x1" %>%</p>
  </div>

  <div class="field">
    <p><%= form.label :after_content %></p>
    <p><%= form.text_area :after_content, id: :comment_after_content, size: "50x6" %></p>
  </div>

  <div class="field">
    <p><%= form.label :after_point %></P>
    <p><%= form.text_area :after_point, id: :comment_after_point, min: 0, max: 100, size: "6x1" %>%</p>
  </div>

  <div class="field">
    <%= form.hidden_field :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
